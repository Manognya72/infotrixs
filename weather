import requests

# Function to get weather data by city name
def get_weather_by_city(city):
    api_key = "YOUR_API_KEY"
    url = f"https://api.weatherapi.com/v1/current.json?key={api_key}&q={city}"
    response = requests.get(url)
    data = response.json()
    # Extract relevant weather information from the response
    temperature = data['current']['temp_c']
    condition = data['current']['condition']['text']
    humidity = data['current']['humidity']
    wind_speed = data['current']['wind_kph']
    # Print the weather information
    print(f"Weather in {city}:")
    print(f"Temperature: {temperature}°C")
    print(f"Condition: {condition}")
    print(f"Humidity: {humidity}%")
    print(f"Wind Speed: {wind_speed} kph")

# Function to add a city to the favorite list
def add_to_favorite(city):
    # Code to add the city to the favorite list

# Function to remove a city from the favorite list
def remove_from_favorite(city):
    # Code to remove the city from the favorite list

# Function to display the favorite list of cities
def display_favorite_list():
    # Code to display the favorite list of cities

# Function to refresh the weather data every 15-30 seconds
def auto_refresh():
    # Code to refresh the weather data every 15-30 seconds

# Main function to handle user input and execute the appropriate functionality
def main():
    while True:
        print("Weather Checking Application")
        print("1. Check weather by city name")
        print("2. Add city to favorite list")
        print("3. Remove city from favorite list")
        print("4. Display favorite list")
        print("5. Auto refresh weather data")
        print("6. Exit")
        choice = input("Enter your choice: ")
        if choice == "1":
            city = input("Enter city name: ")
            get_weather_by_city(city)
        elif choice == "2":
            city = input("Enter city name: ")
            add_to_favorite(city)
        elif choice == "3":
            city = input("Enter city name: ")
            remove_from_favorite(city)
        elif choice == "4":
            display_favorite_list()
        elif choice == "5":
            auto_refresh()
        elif choice == "6":
            break
        else:
            print("Invalid choice. Please try again.")

if __name__ == "__main__":
    main()
